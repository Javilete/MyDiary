error_log  /var/log/nginx/error.log;

events {

}

http {
    server {
      listen 80;
      location / {
        include  /etc/nginx/mime.types;
        root /data/www;
        index index.html;
      }

      location ~ \.(png) {
        root /data/images;
        }
    }
}
